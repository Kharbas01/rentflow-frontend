/* Set this to your deployed Render API URL (no trailing slash) when the
   frontend is hosted separately, e.g. on Vercel. Leave "" when frontend and
   backend share the same origin (e.g. everything on Render). */
window.API_BASE = "https://rentflow-backend-vbha.onrender.com";
